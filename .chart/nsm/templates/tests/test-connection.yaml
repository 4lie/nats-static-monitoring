apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nsm.fullname" . }}-test-connection"
  labels:
    {{- include "nsm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nsm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
